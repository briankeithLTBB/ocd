import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "OCD by Shelbey | Handmade Hypoallergenic Laundry Detergent",
  description:
    "Small-batch laundry detergent handcrafted in Dallas, TX. Hypoallergenic, skin-safe, HE-safe & cold-wash friendly. Starting at $7. No harsh chemicals — just clean clothes.",
};

export default function RootLayout({ children }) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
